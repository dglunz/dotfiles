# ~/.tmuxinator/pana.yml

name: pana
root: ~/Documents/codes/pana
startup_window: api

windows:
  - api:
      root: ~/Documents/codes/pana/pana-api
      layout: main-horizontal
      panes:
        - nvim
        - gulp
  - web:
      layout: main-horizontal
      root: ~/Documents/codes/pana/pana-web
      panes:
        - nvim
        - gulp
  - agent:
      layout: main-horizontal
      root: ~/Documents/codes/pana/pana-agent
      panes:
        - nvim
        - gulp
  - sabre:
      root: ~/Documents/codes/pana/sabre-soap
      panes:
        - nvim
